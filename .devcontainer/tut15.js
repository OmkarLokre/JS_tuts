// object destructuring  

const obj ={
    naam : "Omkar",
    age:14,
    location:"Thane"
}

// now if we want to access the properties of the object we wil.l call it one by one
// alternative method below:

const{naam:name}=obj;

console.log(name);
