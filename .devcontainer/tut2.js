"use strict"; // 
// alert(3+3); // cannot use this as we are not in browser we are using node JS

// Code readibility should be high

//  Data Types:

// number  upto 2 to power of 53
// bigint
// String -> ""
// null: it is not undefinrd ,it is just empty
// undefined 
// symbol => when uniquness is needed

const accountId = 12445;
let email = "omkar@gmail.com";
var accountPassword = true;
let city = "Thane";
let name;

// typeof keyword

console.log(typeof(accountId));
console.log(typeof(accountPassword));
console.log(typeof(email));
console.log(typeof(city));

// type of null is 'object